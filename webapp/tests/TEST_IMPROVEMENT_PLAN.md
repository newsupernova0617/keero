# 테스트 개선 계획

## 🎯 목표

**가짜 테스트 제거 및 서버 액션 통합 테스트 추가**

- 현재: 71개 테스트 (35% 진짜, 42% 반쪽, 23% 가짜)
- 목표: 60개 테스트 (80% 진짜, 20% 개선됨)

---

## 📊 현재 상태 분석

### ✅ 유지할 테스트 (25개)

- SQL Injection 방지 (3개)
- 위험한 쿼리 감지 (6개)
- 읽기 전용 모드 검증 (5개)
- 실제 DB 작업 (4개)
- 쿼리 유효성 검증 (4개)
- 에러 케이스 (3개)

### ⚠️ 개선할 테스트 (30개)

- 권한 검증 메타데이터 테스트 → 서버 액션 테스트로 전환
- 필수 필드 메타데이터 → 실제 유효성 검사 테스트로 전환

### ❌ 제거할 테스트 (16개)

- 백업 파일명 형식 테스트 (테스트 안에서 로직 재구현)
- 백업 메타데이터 구조 (타입 체크로 충분)
- R2 키 생성 (테스트 안에서 재구현)
- 파일 크기 포맷팅 (함수 재정의)
- 메타데이터 일관성 (너무 단순)

---

## 🚀 Phase 1: 가짜 테스트 제거

### 제거할 파일 영역

#### `r2-backup-mock.test.ts` (19개 → 5개로 축소)

**제거:**

- ❌ 백업 파일명 생성 (3개) - 테스트 안에서 재구현
- ❌ R2 키 생성 (2개) - 테스트 안에서 재구현
- ❌ 메타데이터 구조 (2개) - 타입 체크로 충분
- ❌ 파일 크기 포맷팅 (3개) - 함수 재정의
- ❌ 백업 목록 정렬 (2개) - 단순 로직
- ❌ URL 생성 (2개) - 단순 문자열 조합

**유지:**

- ✅ 복원 안전성 (2개) - 중요한 비즈니스 로직
- ✅ 에러 처리 (3개) - 실제 에러 케이스

#### `table-metadata.test.ts` (8개 → 5개로 축소)

**제거:**

- ❌ 존재하지 않는 테이블 조회 (1개) - 너무 단순
- ❌ 메타데이터 일관성 (2개) - 너무 단순

**유지:**

- ✅ 테이블 메타데이터 조회 (5개) - 기본 기능

---

## 🚀 Phase 2: 서버 액션 통합 테스트 추가

### 새로운 테스트 파일: `server-actions.test.ts`

#### 1. 권한 검증 (5개)

```typescript
it('canCreate: false 테이블 생성 시도 → 403')
it('canEdit: false 테이블 수정 시도 → 403')
it('canDelete: false 테이블 삭제 시도 → 403')
it('editable: false 필드 수정 시도 → 400')
it('Admin이 아닌 사용자 접근 → 401')
```

#### 2. 필수 필드 검증 (3개)

```typescript
it('필수 필드 누락 시 → 400')
it('잘못된 데이터 타입 → 400')
it('maxLength 초과 → 400')
```

#### 3. SQL 쿼리 실행기 (5개)

```typescript
it('읽기 전용 모드에서 UPDATE → 403')
it('읽기 전용 모드에서 DELETE → 403')
it('위험한 쿼리에 warning 반환')
it('빈 쿼리 → 400')
it('SELECT 쿼리 → 결과 반환')
```

#### 4. 백업 시스템 (3개)

```typescript
it('백업 생성 → R2 업로드 성공')
it('백업 복원 → 이전 DB 자동 백업')
it('존재하지 않는 백업 복원 → 404')
```

---

## 🚀 Phase 3: 실제 함수 Import 개선

### `crud-security.test.ts` 개선

**변경:**

```typescript
// 현재: 테스트 안에서 함수 재정의
it('파일 크기 포맷팅', () => {
  const formatBytes = (bytes) => { ... }  // ❌
})

// 개선: 실제 함수 import
import { formatBytes } from '$lib/server/utils'
it('파일 크기 포맷팅', () => {
  expect(formatBytes(1024)).toBe('1 KB')  // ✅
})
```

---

## 📈 예상 결과

### 테스트 수

- 현재: 71개
- Phase 1 후: 55개 (-16개 가짜 테스트 제거)
- Phase 2 후: 71개 (+16개 서버 액션 테스트)
- 최종: 71개 (숫자는 같지만 품질은 2배 향상)

### 테스트 품질

| 단계    | 진짜 | 반쪽 | 가짜 |
| ------- | ---- | ---- | ---- |
| 현재    | 35%  | 42%  | 23%  |
| Phase 1 | 45%  | 55%  | 0%   |
| Phase 2 | 65%  | 35%  | 0%   |
| Phase 3 | 80%  | 20%  | 0%   |

### 실제 안전성

- 현재: 35% 보장
- 최종: 80% 보장 (2.3배 향상)

---

## 🎯 실행 순서

### Step 1: 가짜 테스트 제거 (10분)

```bash
# r2-backup-mock.test.ts 14개 테스트 제거
# table-metadata.test.ts 3개 테스트 제거
```

### Step 2: 서버 액션 테스트 추가 (30분)

```bash
# server-actions.test.ts 생성
# 16개 통합 테스트 작성
```

### Step 3: 실제 함수 Import (10분)

```bash
# utils.ts 유틸리티 함수 파일 생성
# 테스트에서 실제 함수 import
```

### Step 4: 테스트 실행 및 검증 (5분)

```bash
npx vitest run tests/**/*.test.ts
```

---

## ✅ 완료 기준

- [ ] Phase 1: 가짜 테스트 16개 제거 완료
- [ ] Phase 2: 서버 액션 테스트 16개 추가 완료
- [ ] Phase 3: 실제 함수 import 완료
- [ ] 모든 테스트 통과 (71개)
- [ ] 테스트 품질 80% 이상 달성

**예상 소요 시간: 55분**
