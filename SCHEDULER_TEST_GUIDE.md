# 스케줄러 테스트 가이드

## 📋 테스트 개요

**목적**: Railway 배포 전 스케줄러 로직 검증  
**간격**: 5분  
**시간**: 30분 (자동 종료)  
**실행 횟수**: 12회 (각 그룹 6회)

---

## 🚀 실행 방법

### 1. 터미널 준비

```bash
cd /home/yj437/coding/aagag_clone/crawler
source venv/bin/activate
```

### 2. 스케줄러 실행

```bash
python3 test_scheduler_5min.py 2>&1 | tee /tmp/scheduler_test.log
```

---

## ⏰ 예상 타임라인

```
00:00 → 그룹1 시작 (루리웹, 오유, 뽐뿌) - 3개 동시
02:30 → 그룹2 시작 (펨코, 웃대, 개드립) - 3개 동시
05:00 → 그룹1 재실행
07:30 → 그룹2 재실행
10:00 → 그룹1 재실행
12:30 → 그룹2 재실행
15:00 → 그룹1 재실행
17:30 → 그룹2 재실행
20:00 → 그룹1 재실행
22:30 → 그룹2 재실행
25:00 → 그룹1 재실행
27:30 → 그룹2 재실행
30:00 → 자동 종료
```

---

## ✅ 확인 사항

### 스케줄러 로직:

- [ ] 정시에 시작하는가?
- [ ] 그룹1과 그룹2가 2.5분 차이로 실행되는가?
- [ ] 5분마다 반복 실행되는가?
- [ ] 30분 후 자동 종료되는가?

### 병렬 실행:

- [ ] 3개 사이트가 동시에 실행되는가?
- [ ] 같은 사이트가 중복 실행되지 않는가?
- [ ] 한 사이트 에러가 다른 사이트에 영향 없는가?

### 크롤링 성능:

- [ ] 매번 새 게시글을 수집하는가?
- [ ] API 모드가 정상 작동하는가?
- [ ] 이미지가 정상 저장되는가?
- [ ] 중복 게시글이 스킵되는가?

---

## 📊 로그 확인

### 실시간 로그:

```bash
tail -f /tmp/scheduler_test.log
```

### 특정 사이트 로그:

```bash
grep "ppomppu" /tmp/scheduler_test.log
```

### 에러 확인:

```bash
grep "ERROR\|Failed" /tmp/scheduler_test.log
```

### 성공 확인:

```bash
grep "✅ Completed" /tmp/scheduler_test.log | wc -l
```

---

## 🛑 중단 방법

### 수동 중단:

```
Ctrl + C
```

### 자동 종료:

- 30분 후 자동으로 종료됩니다.

---

## 📈 예상 결과

### 성공 시:

```
총 실행: 12회
새 게시글: ~30-60개 (사이트당 5-10개)
중복 스킵: ~10-20개
에러: 0개
```

### 실패 시:

- 로그에서 에러 메시지 확인
- 특정 사이트만 실패하는지 확인
- API 연결 문제인지 확인

---

## 🔧 문제 해결

### API 연결 실패:

```bash
# SvelteKit 서버 확인
curl http://localhost:5173
```

### 중복만 계속 나올 때:

- 정상입니다! 5분 간격이라도 새 글이 없을 수 있음
- 다음 실행을 기다리세요

### 특정 사이트 실패:

- 해당 사이트 접속 확인
- Playwright 설치 확인
- 로그에서 상세 에러 확인

---

## 📝 테스트 후 체크리스트

- [ ] 12회 모두 실행되었는가?
- [ ] 에러 없이 완료되었는가?
- [ ] 새 게시글이 수집되었는가?
- [ ] DB에 정상 저장되었는가?
- [ ] 로그가 정상 기록되었는가?

**모두 체크되면 Railway 배포 준비 완료!** 🎉

---

## 🚀 다음 단계

테스트 성공 시:

1. `scheduler.py`를 10분 간격으로 복원
2. Railway에 배포
3. 환경변수 설정
4. 스케줄러 서비스 시작

---

**작성일**: 2025-12-29  
**테스트 시간**: 30분  
**예상 성공률**: 95%+
